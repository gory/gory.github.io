ES6

classes w/ extends

getter 'get'
setter 'set'
delete

template strings?

arguments
default x=12
rest	x...
spread 
let x = [1,2, 3]
f(...x) does f(1, 2, 3)

let/const

//need polyfill for babel
iterators next => {value: "somevalue", done: boolean}
	iterables 
		Symbol.iterator = function* () {};
		built-in : [String, Array, TypedArray, Map, Set]
		for .. of

generators

function* gen() {
  yield* ['a', 'b', 'c'];
}

gen().next(); // { value: "a", done: false }


modules : import/export

Set WeakSet Map Weak Map

proxy
var p = new Proxy(target, handler);

Symbols

Tail Calls

